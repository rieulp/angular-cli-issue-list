import { NotFoundWrapper } from './styles';

const NotFound = () => {
  return (
    <NotFoundWrapper>
      <h1>NOT FOUND</h1>
      <button>뒤로가기</button>
    </NotFoundWrapper>
  );
};

export default NotFound;
